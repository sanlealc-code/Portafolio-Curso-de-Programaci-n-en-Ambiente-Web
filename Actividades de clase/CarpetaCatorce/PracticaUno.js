console.log("Hello, World!");
 /*
var nombreSoda = "La Sodita UTN"; //Primer var
var pedidos = []; //Segundo var
 
function crearPedido(cliente, producto, precio, notas) {
  var pedido = { //Tercer var
    cliente: cliente,
    producto: producto,
    precio: precio,
    notas: notas
  };
  pedidos.push(pedido);
  console.log("Pedido creado para " + cliente + ": " + producto + " - " + precio); 
//5 concatenaciones con + en la línea 13


  return pedido;
}

function calcularTotalDia() {
  var total = 0; // cuarto var 
  for (var i = 0; i < pedidos.length; i++) { 7// quinto var
    total = total + pedidos[i].precio;
// 1 concatenaciones con + en la línea 20
//6 concatenaciones totales

  }
  return total;
}


function aplicarDescuento(pedido, porcentaje) {
  pedido.precio = pedido.precio - (pedido.precio * porcentaje / 100);
  return pedido;
}
 
crearPedido("Ana", "Casado", 2500, "Sin cebolla");
crearPedido("Luis", "Cafe con pan", 1200, undefined);

*/





